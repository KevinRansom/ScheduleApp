<Window x:Class="ScheduleApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:ScheduleApp.Converters"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Support Coverage Scheduler"
        Height="800"
        Width="1200"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
  <Window.Resources>
    <!-- Keep converters and non-appearance resources here -->
    <conv:HeaderCornerRadiusConverter x:Key="HeaderCornerRadiusConverter"/>
    <conv:HeaderBorderThicknessConverter x:Key="HeaderBorderThicknessConverter"/>
    <conv:RoundedClipConverter x:Key="RoundedClipConverter"/>

    <conv:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter" />
    <conv:TaskKindToBrushConverter x:Key="TaskKindToBrushConverter"
                                   CoverageBrush="{StaticResource BrushCoverage}"
                                   BreakBrush="{StaticResource BrushBreak}"
                                   LunchBrush="{StaticResource BrushLunch}"
                                   IdleBrush="{StaticResource BrushIdle}"/>
    <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    <conv:InverseBooleanToVisibilityConverter x:Key="BoolToVisInverse"/>
    <conv:NotNullToTrueConverter x:Key="NotNullToTrue"/>
    <conv:NotNullToFalseConverter x:Key="NotNullToFalse"/>

    <!-- Time item template -->
    <DataTemplate x:Key="TimeItemTemplate">
      <TextBlock Text="{Binding Converter={StaticResource TimeSpanToStringConverter}}" />
    </DataTemplate>

    <!-- Use centralized DataGrid element styles from Appearance.xaml -->
    <Style x:Key="PillStyle" TargetType="Border" BasedOn="{StaticResource PillStyle}"/>
  </Window.Resources>

  <DockPanel LastChildFill="True">
    <!-- Top-aligned hamburger menu (docked top). Use centralized metrics -->
    <Border DockPanel.Dock="Top"
            CornerRadius="{StaticResource TopBarCornerRadius}"
            Background="{DynamicResource MenuBackgroundBrush}"
            BorderBrush="{DynamicResource MenuBorderBrush}"
            BorderThickness="{StaticResource GridOuterBorderThickness}"
            Padding="{StaticResource TopBarPadding}"
            SnapsToDevicePixels="True"
            Margin="{StaticResource TopBarMargin}">
      <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <!-- Use centralized hamburger style -->
          <ToggleButton x:Name="HamburgerToggle"
                        Style="{StaticResource HamburgerToggleButtonStyle}"/>

          <Popup x:Name="HamburgerPopup"
                 PlacementTarget="{Binding ElementName=HamburgerToggle}"
                 Placement="Bottom"
                 AllowsTransparency="True"
                 StaysOpen="False"
                 IsOpen="{Binding IsChecked, ElementName=HamburgerToggle, Mode=TwoWay}"
                 Closed="HamburgerPopup_Closed">
            <Border CornerRadius="{StaticResource PopupCornerRadius}"
                    Background="{DynamicResource MenuBackgroundBrush}"
                    BorderBrush="{DynamicResource MenuBorderBrush}"
                    BorderThickness="{StaticResource GridOuterBorderThickness}"
                    Padding="{StaticResource PopupPadding}"
                    SnapsToDevicePixels="True"
                    Margin="{StaticResource PopupMargin}"
                    Effect="{StaticResource PopupShadowEffect}">
              <StackPanel Orientation="Vertical" MinWidth="200">
                <Button Content="Schedule" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuSchedule_Click"/>
                <Button Content="Preview" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuPreview_Click"/>
                <Button Content="Teachers" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuTeachers_Click"/>
                <Button Content="School" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuSchool_Click"/>
                <Button Content="Preferences" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuPreferences_Click"/>
                <Button Content="Algorithm" Style="{StaticResource IconButton}" Margin="0,0,0,6" Click="MenuAlgorithm_Click"/>
                <Separator Margin="0,6,0,6"/>
                <Button Content="About" Style="{StaticResource IconButton}" Click="AboutMenu_Click"/>
              </StackPanel>
            </Border>
          </Popup>
        </Grid>

        <TabControl x:Name="MainTabControl" SelectedIndex="{Binding SelectedTabIndex}">
          <TabItem Header="Setup">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
              <Grid Margin="16">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Simple title (centralized font size) -->
                <TextBlock Text="Setup" FontSize="{StaticResource PageTitleFontSize}" Margin="0,0,0,12"/>

                <!-- Two columns: left = tabbed data grids, right = school + actions -->
                <Grid Grid.Row="1">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="420" />
                  </Grid.ColumnDefinitions>

                  <TabControl x:Name="SetupInnerTab" Grid.Column="0" Margin="0,0,20,0">
                    <TabItem Header="Classrooms and Teachers">
                      <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ContentControl Grid.Column="0"
                                        Style="{StaticResource RoundedTableContainer}"
                                        Margin="0">

                            <DataGrid x:Name="TeachersGrid"
                                      ItemsSource="{Binding Setup.Teachers}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserSortColumns="True"
                                      HeadersVisibility="Column"
                                      SelectionMode="Extended"
                                      SelectionUnit="FullRow"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                                      SnapsToDevicePixels="True"
                                      BorderThickness="0"
                                      Background="Transparent"
                                      ColumnHeaderStyle="{StaticResource RoundedDataGridColumnHeader}">

                                <DataGrid.Resources>
                                    <Style TargetType="{x:Type DataGridColumnHeadersPresenter}">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                                        <Setter Property="ClipToBounds" Value="True"/>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Room Number"
                                                        Binding="{Binding RoomNumber}"
                                                        SortMemberPath="RoomNumber"
                                                        Width="*"
                                                        CellStyle="{StaticResource DefaultCellStyle}"
                                                        ElementStyle="{StaticResource DefaultTextElementStyle}"
                                                        EditingElementStyle="{StaticResource DefaultEditingElementStyle}"/>

                                    <DataGridTextColumn Header="Teacher Name"
                                                        Binding="{Binding Name}"
                                                        SortMemberPath="Name"
                                                        Width="*"
                                                        CellStyle="{StaticResource DefaultCellStyle}"
                                                        ElementStyle="{StaticResource DefaultTextElementStyle}"
                                                        EditingElementStyle="{StaticResource DefaultEditingElementStyle}"/>

                                    <DataGridTemplateColumn Header="Start Time"
                                                            SortMemberPath="Start"
                                                            Width="*"
                                                            CellStyle="{StaticResource DefaultCellStyle}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Start, Converter={StaticResource TimeSpanToStringConverter}}"
                                                           Style="{StaticResource DefaultTextElementStyle}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding DataContext.QuarterHours, RelativeSource={RelativeSource AncestorType=Window}}"
                                                          SelectedItem="{Binding Start, Mode=TwoWay}"
                                                          ItemTemplate="{StaticResource TimeItemTemplate}"
                                                          MinWidth="120"
                                                          Padding="{StaticResource CellEditorPadding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="End Time"
                                                            SortMemberPath="End"
                                                            Width="*"
                                                            CellStyle="{StaticResource LastColumnCellStyle}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding End, Converter={StaticResource TimeSpanToStringConverter}}"
                                                           Style="{StaticResource DefaultTextElementStyle}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding DataContext.QuarterHours, RelativeSource={RelativeSource AncestorType=Window}}"
                                                          SelectedItem="{Binding End, Mode=TwoWay}"
                                                          ItemTemplate="{StaticResource TimeItemTemplate}"
                                                          MinWidth="120"
                                                          Padding="{StaticResource CellEditorPadding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ContentControl>

                        <StackPanel Grid.Column="1"
                                    Orientation="Vertical"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Top">
                          <Button Content="Add"
                                  Style="{StaticResource RoundedActionButton}"
                                  Margin="0,0,0,12"
                                  Command="{Binding Setup.AddTeacherCommand}"/>
                          <Button Content="Remove"
                                  Style="{StaticResource RoundedActionButton}"
                                  Command="{Binding Setup.RemoveTeacherCommand}"
                                  CommandParameter="{Binding SelectedItems, ElementName=TeachersGrid}"/>
                        </StackPanel>
                      </Grid>
                    </TabItem>

                    <TabItem Header="Support Staff">
                      <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ContentControl Grid.Column="0" Style="{StaticResource RoundedTableContainer}" Margin="0">
      <DataGrid x:Name="SupportsGrid"
                ItemsSource="{Binding Setup.Supports}"
                SelectedItem="{Binding Setup.SelectedSupport}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserSortColumns="True"
                HeadersVisibility="Column"
                SelectionMode="Extended"
                SelectionUnit="FullRow"
                GridLinesVisibility="Horizontal"
                HorizontalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                SnapsToDevicePixels="True"
                BorderThickness="0"
                Background="Transparent"
                ColumnHeaderStyle="{StaticResource RoundedDataGridColumnHeader}">

        <DataGrid.Resources>
          <Style TargetType="{x:Type DataGridColumnHeadersPresenter}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ClipToBounds" Value="True"/>
          </Style>
        </DataGrid.Resources>

        <DataGrid.Columns>
          <DataGridTextColumn Header="Room Number"
                              Binding="{Binding Name}"
                              SortMemberPath="Name"
                              Width="*"
                              CellStyle="{StaticResource DefaultCellStyle}">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="{StaticResource CellElementMargin}"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
            <DataGridTextColumn.EditingElementStyle>
              <Style TargetType="TextBox">
                <Setter Property="Padding" Value="{StaticResource CellEditorPadding}"/>
              </Style>
            </DataGridTextColumn.EditingElementStyle>
          </DataGridTextColumn>

          <DataGridTemplateColumn Header="Start Time"
                                  SortMemberPath="Start"
                                  Width="*"
                                  CellStyle="{StaticResource DefaultCellStyle}">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Start, Converter={StaticResource TimeSpanToStringConverter}}"
                           Style="{StaticResource DefaultTextElementStyle}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
            <DataGridTemplateColumn.CellEditingTemplate>
              <DataTemplate>
                <ComboBox ItemsSource="{Binding DataContext.QuarterHours, RelativeSource={RelativeSource AncestorType=Window}}"
                          SelectedItem="{Binding Start, Mode=TwoWay}"
                          ItemTemplate="{StaticResource TimeItemTemplate}"
                          MinWidth="120"
                          Padding="{StaticResource CellEditorPadding}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellEditingTemplate>
          </DataGridTemplateColumn>

          <DataGridTemplateColumn Header="End Time"
                                  SortMemberPath="End"
                                  Width="*"
                                  CellStyle="{StaticResource LastColumnCellStyle}">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding End, Converter={StaticResource TimeSpanToStringConverter}}"
                           Style="{StaticResource DefaultTextElementStyle}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
            <DataGridTemplateColumn.CellEditingTemplate>
              <DataTemplate>
                <ComboBox ItemsSource="{Binding DataContext.QuarterHours, RelativeSource={RelativeSource AncestorType=Window}}"
                          SelectedItem="{Binding End, Mode=TwoWay}"
                          ItemTemplate="{StaticResource TimeItemTemplate}"
                          MinWidth="120"
                          Padding="{StaticResource CellEditorPadding}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellEditingTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </ContentControl>

    <StackPanel Grid.Column="1"
                Orientation="Vertical"
                Margin="12,0,0,0"
                VerticalAlignment="Top">
      <Button Content="Add"
              Style="{StaticResource RoundedActionButton}"
              Margin="0,0,0,12"
              Command="{Binding Setup.AddSupportCommand}"/>
      <Button Content="Remove"
              Style="{StaticResource RoundedActionButton}"
              Command="{Binding Setup.RemoveSupportCommand}"
              CommandParameter="{Binding SelectedItems, ElementName=SupportsGrid}"/>
    </StackPanel>
  </Grid>
</TabItem>

                    <TabItem Header="Classroom Support Preferences">
                      <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ContentControl Grid.Column="0" Style="{StaticResource RoundedTableContainer}" Margin="0">
      <DataGrid x:Name="PrefsGrid"
                ItemsSource="{Binding Setup.Preferences}"
                SelectedItem="{Binding Setup.SelectedPreference}"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserSortColumns="True"
                HeadersVisibility="Column"
                SelectionMode="Extended"
                SelectionUnit="FullRow"
                GridLinesVisibility="Horizontal"
                HorizontalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                SnapsToDevicePixels="True"
                BorderThickness="0"
                Background="Transparent"
                ColumnHeaderStyle="{StaticResource RoundedDataGridColumnHeader}">

        <DataGrid.Resources>
          <Style TargetType="{x:Type DataGridColumnHeadersPresenter}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ClipToBounds" Value="True"/>
          </Style>
        </DataGrid.Resources>

        <DataGrid.Columns>
          <DataGridTextColumn Header="Room Number"
                              Binding="{Binding RoomNumber}"
                              SortMemberPath="RoomNumber"
                              Width="*"
                              CellStyle="{StaticResource DefaultCellStyle}"
                              ElementStyle="{StaticResource DefaultTextElementStyle}"
                              EditingElementStyle="{StaticResource DefaultEditingElementStyle}"/>

          <DataGridTemplateColumn Header="Preferred Support Staff"
                                  SortMemberPath="PreferredSupportName"
                                  Width="*"
                                  CellStyle="{StaticResource LastColumnCellStyle}">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding PreferredSupportName}" Margin="{StaticResource CellElementMargin}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
            <DataGridTemplateColumn.CellEditingTemplate>
              <DataTemplate>
                <ComboBox ItemsSource="{Binding DataContext.Setup.Supports, RelativeSource={RelativeSource AncestorType=Window}}"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Name"
                          SelectedValue="{Binding PreferredSupportName}"
                          Margin="{StaticResource CellElementMargin}"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellEditingTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </ContentControl>

    <StackPanel Grid.Column="1"
                Orientation="Vertical"
                Margin="12,0,0,0"
                VerticalAlignment="Top">
      <Button Content="Add"
              Style="{StaticResource RoundedActionButton}"
              Margin="0,0,0,12"
              Command="{Binding Setup.AddPreferenceCommand}"/>
      <Button Content="Remove"
              Style="{StaticResource RoundedActionButton}"
              Command="{Binding Setup.RemovePreferenceCommand}"
              CommandParameter="{Binding SelectedItems, ElementName=PrefsGrid}"/>
    </StackPanel>
  </Grid>
</TabItem>
                  </TabControl>

                  <Grid Grid.Column="1" Margin="0,0,0,0">
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Header="School" Grid.Row="0" Margin="0,0,0,24">
                      <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto"/>
                          <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                          <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="School Name"/>
                        <TextBox   Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                                   Text="{Binding Setup.SchoolName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Top" Text="Address"/>
                        <TextBox   Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                                   Text="{Binding Setup.SchoolAddress, UpdateSourceTrigger=PropertyChanged}"
                                   AcceptsReturn="True"
                                   TextWrapping="Wrap"
                                   VerticalScrollBarVisibility="Auto"
                                   MinLines="4"
                                   MaxLines="4"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="Telephone"/>
                        <TextBox   Grid.Row="2" Grid.Column="1"
                                   Text="{Binding Setup.SchoolPhone, UpdateSourceTrigger=PropertyChanged}"/>
                      </Grid>
                    </GroupBox>

                    <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                      <Button Content="Generate Schedule"
                              Style="{StaticResource IconButton}"
                              MinWidth="260" Height="44"
                              Margin="0,0,0,16"
                              Command="{Binding GenerateScheduleCommand}"/>
                      <Button Content="Load Data"
                              Style="{StaticResource IconButton}"
                              MinWidth="260" Height="44"
                              Margin="0,0,0,16"
                              Command="{Binding LoadSetupCommand}"/>
                      <Button Content="Save Data"
                              Style="{StaticResource IconButton}"
                              MinWidth="260" Height="44"
                              Margin="0,0,0,0"
                              Command="{Binding SaveSetupCommand}"/>
                    </StackPanel>
                  </Grid>
                </Grid>
              </Grid>
            </ScrollViewer>
          </TabItem>

          <TabItem Header="Schedule View">
            <Grid Margin="8">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                  <Button Content="Save…" Style="{StaticResource IconButton}"
                          Command="{Binding SaveScheduleCommand}"
                          Margin="0,0,0,0"/>
                  <TextBlock Text="Each tab shows a support's day. Waiting time counts as working time."
                             VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                  <Border Background="{StaticResource BrushCoverage}" Style="{StaticResource PillStyle}">
                    <TextBlock Text="Coverage" />
                  </Border>
                  <Border Background="{StaticResource BrushBreak}" Style="{StaticResource PillStyle}">
                    <TextBlock Text="Break (10m + 5m buffer)" />
                  </Border>
                  <Border Background="{StaticResource BrushLunch}" Style="{StaticResource PillStyle}">
                    <TextBlock Text="Lunch (30m)" />
                  </Border>
                  <Border Background="{StaticResource BrushIdle}" Style="{StaticResource PillStyle}">
                    <TextBlock Text="Free" />
                  </Border>
                  <Border Background="{StaticResource BrushUnscheduled}" Style="{StaticResource PillStyle}">
                    <TextBlock Text="Unscheduled" />
                  </Border>
                </StackPanel>
              </Grid>

              <TabControl Grid.Row="1">
                <TabItem Header="By Support">
                  <TabControl ItemsSource="{Binding Schedule.SupportTabs}">
                    <TabControl.ItemTemplate>
                      <DataTemplate>
                        <TextBlock Text="{Binding SupportName}"/>
                      </DataTemplate>
                    </TabControl.ItemTemplate>

                    <TabControl.ContentTemplate>
                      <DataTemplate>
                        <Grid>
                          <Grid x:Name="VisualView"
                                Visibility="{Binding DataContext.Schedule.ShowVisual, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                              <Grid Margin="0" Grid.IsSharedSizeScope="True">
                                <Grid.RowDefinitions>
                                  <RowDefinition Height="Auto"/>
                                  <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Padding="6" Margin="2,0,2,6"
                                        BorderBrush="{StaticResource SchedGridLineBrush}" BorderThickness="0,0,0,1" Background="{StaticResource PrimaryBackgroundBrush}">
                                  <Grid>
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition SharedSizeGroup="cSupport"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition SharedSizeGroup="cTask"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition SharedSizeGroup="cDur"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition SharedSizeGroup="cTeacher"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition SharedSizeGroup="cRoom"/>
                                      <ColumnDefinition Width="Auto"/>
                                      <ColumnDefinition SharedSizeGroup="cStart"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Support"/>
                                    <TextBlock Grid.Column="1" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="  |  "/>
                                    <TextBlock Grid.Column="2" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Task"/>
                                    <TextBlock Grid.Column="3" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="  |  "/>
                                    <TextBlock Grid.Column="4" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Duration"/>
                                    <TextBlock Grid.Column="5" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="  |  "/>
                                    <TextBlock Grid.Column="6" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Teacher"/>
                                    <TextBlock Grid.Column="7" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="  |  "/>
                                    <TextBlock Grid.Column="8" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Room"/>
                                    <TextBlock Grid.Column="9" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="  |  "/>
                                    <TextBlock Grid.Column="10" Style="{StaticResource MonospaceText}" FontWeight="Bold" Text="Start"/>
                                  </Grid>
                                </Border>

                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Tasks}">
                                  <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                      <Border Margin="2" Padding="6"
                                              Background="{Binding Kind, Converter={StaticResource TaskKindToBrushConverter}}">
                                        <Grid>
                                          <Grid.ColumnDefinitions>
                                            <ColumnDefinition SharedSizeGroup="cSupport"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition SharedSizeGroup="cTask"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition SharedSizeGroup="cDur"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition SharedSizeGroup="cTeacher"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition SharedSizeGroup="cRoom"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition SharedSizeGroup="cStart"/>
                                          </Grid.ColumnDefinitions>

                                          <TextBlock Grid.Column="0" Style="{StaticResource MonospaceText}" Text="{Binding SupportName}"/>
                                          <TextBlock Grid.Column="1" Style="{StaticResource MonospaceText}" Text="  |  " />
                                          <TextBlock Grid.Column="2" Style="{StaticResource MonospaceText}" Text="{Binding TaskName}"/>
                                          <TextBlock Grid.Column="3" Style="{StaticResource MonospaceText}" Text="  |  " />
                                          <TextBlock Grid.Column="4" Style="{StaticResource MonospaceText}" Text="{Binding DurationText}"/>
                                          <TextBlock Grid.Column="5" Style="{StaticResource MonospaceText}" Text="  |  " />
                                          <TextBlock Grid.Column="6" Style="{StaticResource MonospaceText}" Text="{Binding TeacherDisplay}"/>
                                          <TextBlock Grid.Column="7" Style="{StaticResource MonospaceText}" Text="  |  " />
                                          <TextBlock Grid.Column="8" Style="{StaticResource MonospaceText}" Text="{Binding RoomDisplay}"/>
                                          <TextBlock Grid.Column="9" Style="{StaticResource MonospaceText}" Text="  |  " />
                                          <TextBlock Grid.Column="10" Style="{StaticResource MonospaceText}" Text="{Binding StartText}"/>
                                        </Grid>
                                      </Border>
                                    </DataTemplate>
                                  </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                          </ScrollViewer>
                        </Grid>

                        <Grid x:Name="GridView"
                              Visibility="{Binding DataContext.Schedule.ShowGrid, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}">
                          <Border Style="{StaticResource RoundedTableBorder}" Margin="0">
                            <DataGrid ItemsSource="{Binding Tasks}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      FontFamily="Consolas"
                                      GridLinesVisibility="All"
                                      HorizontalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                                      VerticalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                                      ColumnHeaderStyle="{StaticResource SchedGridHeaderStyleDefault}"
                                      CellStyle="{StaticResource SchedGridCellStyle}"
                                      BorderThickness="0"
                                      Background="Transparent"
                                      SnapsToDevicePixels="True"
                                      Margin="0">

                                <DataGrid.RowStyle>
                                  <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="{Binding Kind, Converter={StaticResource TaskKindToBrushConverter}}"/>
                                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                                  </Style>
                                </DataGrid.RowStyle>

                                <DataGrid.Columns>
                                  <DataGridTextColumn Header="Support"  Binding="{Binding SupportName}"    Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>

                                  <DataGridTextColumn Header="Task"     Binding="{Binding TaskName}"       Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>

                                  <DataGridTextColumn Header="Duration" Binding="{Binding DurationText}"   Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>

                                  <DataGridTextColumn Header="Teacher"  Binding="{Binding TeacherDisplay}" Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>

                                  <DataGridTextColumn Header="Room"     Binding="{Binding RoomDisplay}"    Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>

                                  <DataGridTextColumn Header="Start"    Binding="{Binding StartText}"      Width="*" >
                                    <DataGridTextColumn.ElementStyle>
                                      <StaticResource ResourceKey="SchedGridCellText"/>
                                    </DataGridTextColumn.ElementStyle>
                                  </DataGridTextColumn>
                                </DataGrid.Columns>
                              </DataGrid>
                            </Border>
                          </Grid>
                        </Grid>
                      </DataTemplate>
                    </TabControl.ContentTemplate>
                  </TabControl>
                </TabItem>

                <TabItem Header="By Teacher">
                  <Grid Margin="0">
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
                      <TextBlock Text="Teacher:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                      <ComboBox Width="240"
                                ItemsSource="{Binding Setup.Teachers}"
                                DisplayMemberPath="Name"
                                SelectedItem="{Binding Schedule.SelectedTeacher}"/>
                    </StackPanel>

                    <Border Grid.Row="1" Style="{StaticResource RoundedTableBorder}" Margin="0">
                      <DataGrid ItemsSource="{Binding Schedule.SelectedTeacherRows}"
                                AutoGenerateColumns="False"
                                CanUserAddRows="False"
                                CanUserDeleteRows="False"
                                IsReadOnly="True"
                                HeadersVisibility="Column"
                                GridLinesVisibility="All"
                                HorizontalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                                VerticalGridLinesBrush="{StaticResource SchedGridLineBrush}"
                                ColumnHeaderStyle="{StaticResource SchedGridHeaderStyleDefault}"
                                CellStyle="{StaticResource SchedGridCellStyle}"
                                BorderThickness="0"
                                Background="Transparent"
                                SnapsToDevicePixels="True"
                                Margin="0">
                    <DataGrid.RowStyle>
                      <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding Converter={StaticResource TeacherRowToBrushConverter}}"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                      </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                      <DataGridTextColumn Header="Name"         Binding="{Binding TeacherName}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                          <StaticResource ResourceKey="SchedGridCellText"/>
                        </DataGridTextColumn.ElementStyle>
                      </DataGridTextColumn>
                      <DataGridTextColumn Header="Support Staff" Binding="{Binding SupportStaff}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                          <StaticResource ResourceKey="SchedGridCellText"/>
                        </DataGridTextColumn.ElementStyle>
                      </DataGridTextColumn>
                      <DataGridTextColumn Header="Activity"     Binding="{Binding Activity}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                          <StaticResource ResourceKey="SchedGridCellText"/>
                        </DataGridTextColumn.ElementStyle>
                      </DataGridTextColumn>
                      <DataGridTextColumn Header="Duration"     Binding="{Binding Duration}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                          <StaticResource ResourceKey="SchedGridCellText"/>
                        </DataGridTextColumn.ElementStyle>
                      </DataGridTextColumn>
                      <DataGridTextColumn Header="Start"        Binding="{Binding Start}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                          <StaticResource ResourceKey="SchedGridCellText"/>
                        </DataGridTextColumn.ElementStyle>
                      </DataGridTextColumn>
                    </DataGrid.Columns>
                  </DataGrid>
                </Border>
              </Grid>
            </TabItem>
          </TabControl>
        </Grid>
      </TabItem>

      <TabItem Header="Print Preview">
        <Grid Margin="8">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <ComboBox Width="200"
                      ItemsSource="{Binding Schedule.SupportTabs}"
                      DisplayMemberPath="SupportName"
                      SelectedItem="{Binding PrintPreview.SelectedTab}" />
            <Button Content="Print All" Style="{StaticResource IconButton}" Command="{Binding PrintPreview.PrintAllCommand}" Margin="8,0,0,0"/>
            <Button Content="Print Selected" Style="{StaticResource IconButton}" Command="{Binding PrintPreview.PrintSelectedCommand}" Margin="4,0,0,0"
                    IsEnabled="{Binding PrintPreview.SelectedTab, Converter={StaticResource NotNullToTrue}}"/>
            <Button Content="Export PDF" Style="{StaticResource IconButton}" Command="{Binding PrintPreview.ExportPdfCommand}" Margin="4,0,0,0"
                    IsEnabled="{Binding PrintPreview.SelectedTab, Converter={StaticResource NotNullToTrue}}"/>
          </StackPanel>

          <FlowDocumentScrollViewer Grid.Row="1" Document="{Binding PrintPreview.Document}" />
        </Grid>
      </TabItem>
    </TabControl>
      </DockPanel>
    </Border>

    <!-- No other content below; main TabControl has been moved into the top Border per Option A -->
  </DockPanel>
</Window>
